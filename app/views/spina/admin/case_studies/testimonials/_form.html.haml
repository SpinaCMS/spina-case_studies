- if @testimonial.errors.any?
  - content_for :notifications do
    .notification.notification-danger.animated.fadeInRight
      = icon('exclamation')
      .notification-message
        =t 'spina.notifications.alert'
        %small= @testimonial.errors.full_messages.join('<br />').html_safe
      = link_to '#', data: {close_notification: true} do
        = icon('cross')

%header#header
  .breadcrumbs= render_breadcrumbs separator: '<div class="divider"></div>'
  - unless @testimonial.new_record?
    - if Spina.config.locales.size > 1
      %div{style: 'display: inline-block; margin-left: 12px; top: -4px', data: {dropdown: true}}
        = link_to '#', class: 'button button-link button-round button-small', data: {trigger: 'dropdown', target: '#locales'} do
          = icon('comment')
          = @locale.upcase

        %ul#locales
          - Spina.config.locales.each do |locale|
            %li
              = link_to t("languages.#{locale}"), "?locale=#{locale}", style: ('font-weight: 600' if @locale.to_s == locale.to_s)

  #header_actions
    %button.button.button-primary{type: 'submit', style: 'margin-right: 0', data: {disable_with: t('spina.case_studies.testimonials.saving')}}
      = icon('check')
      =t 'spina.case_studies.testimonials.save'

  %nav#secondary.tabs
    %ul
      - @tabs.each_with_index do |tab, i|
        %li{class: (i==0 ? 'active' : '')}
          = link_to t("spina.case_studies.testimonials.#{tab}"), "##{tab}"

= hidden_field_tag :locale, @locale

- Mobility.with_locale(@locale) do
  - @tabs.each do |tab|
    = render "spina/admin/case_studies/testimonials/form_#{tab}", f: f

  - unless @testimonial.new_record?
    .pull-right= link_to t('spina.permanently_delete'), spina.admin_case_studies_testimonial_path(@testimonial), method: :delete, data: {confirm: t('spina.case_studies.testimonials.delete_confirmation', subject: @testimonial.name)}, class: 'button button-link button-danger'
